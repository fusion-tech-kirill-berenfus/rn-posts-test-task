import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginTop: 20,
    marginHorizontal: 40,
  },
  title: {
    fontSize: 20,
    fontWeight: 'bold',
  },
});

export default styles;
